name: omni-pixel

services:
  db:
    image: postgres:18
    environment:
      POSTGRES_DB: omni_pixel
      POSTGRES_USER: pixel
      POSTGRES_PASSWORD: "123456"
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql

volumes:
  postgres-data: